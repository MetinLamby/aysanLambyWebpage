<div id="home-div">
    <div id="images-div" class="slideshow">
        <%= cl_image_tag("sl1", {class: "firstimage"})%>
        <%= cl_image_tag("sl2", {class: "image"})%>
        <%= cl_image_tag("sl3", {class: "image"})%>
        <%= cl_image_tag("sl4", {class: "image"})%>
        <%= cl_image_tag("sl5", {class: "image"})%>
        <%= cl_image_tag("sl6", {class: "image"})%>
        <%= cl_image_tag("sl7", {class: "image"})%>
        <%= cl_image_tag("sl8", {class: "image"})%>
        <%= cl_image_tag("sl9", {class: "image"})%>
        <%= cl_image_tag("sl10", {class: "image"})%>
        <%= cl_image_tag("sl11", {class: "image"})%>
        <%= cl_image_tag("sl12", {class: "image"})%>
        <%= cl_image_tag("sl13", {class: "image"})%>
        <%= cl_image_tag("sl14", {class: "image"})%>
        <%= cl_image_tag("sl15", {class: "image"})%>
        <%= cl_image_tag("sl16", {class: "image"})%>
    </div>
    <div id="buttons-div">
        <div id="photos-div" class="div-one-part"><a href="<%= photos_path %>">photos</a></div>
        <div id="videos-div" class="div-one-part"><a href="<%= videos_path %>">videos</a></div>
    </div>

<script>

// Get the slideshow container
  var slideshow = document.querySelector('.slideshow');

  // Get the images
  var images = slideshow.querySelectorAll('img');

  // Set the index to the first image
  var index = 0;

  // Set the interval for the slideshow
  var interval = setInterval(showNextImage, 3000);

  function showNextImage() {
      // Hide the current image
      images[index].style.display = 'none';

      // Increment the index
      index = (index + 1) % images.length;

      // Show the next image
      images[index].style.display = 'block';
  }
</script>

<div id="footer-div">
    <p><a href = "mailto: aysanlamby@outlook.com">contact</a> | <a href="https://www.instagram.com/aysanlamby/?hl=en">instagram</a></p>
</div>

</div>

